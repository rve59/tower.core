[project]
name = "tower-data-engine"
version = "0.1.0"
description = "TOWER Polars Data Engine"
authors = [{ name = "Raynier" }]
requires-python = ">=3.14"
dependencies = [
    "polars[gpu]",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
